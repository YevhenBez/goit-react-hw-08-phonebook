import {AppBar, Button, Box, Container, Toolbar, Typography } from '@mui/material';

const MaterialUI = {
  AppBar,
  Button,
  Box,
  Container,
  Toolbar,
  Typography,
};
export default MaterialUI;
